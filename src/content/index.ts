/**
 * Content script — внедряется в страницы GitLab
 * TODO: контекстное меню, инъекция BPMN-превью
 */
function init(): void {
  console.log("[GitLab BPMN Viewer] Content script loaded");
}

init();
